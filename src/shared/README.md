# Shared Layer

`src/shared/` is reserved for explicit shared concerns only.

- `auth` for reusable auth helpers and guards
- `db` and `supabase` for infrastructure access
- `ui` for UI primitives
- `validation` for truly cross-domain validation
- `lib` for generic utilities
