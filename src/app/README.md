# App Router

`src/app/` contains route segments, layouts, route handlers, and page-level composition only.

- `(auth)` holds authentication-facing routes
- `(app)` holds authenticated application routes
- `api` holds route handlers and server entrypoints
