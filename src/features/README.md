# Feature Slices

`src/features/` contains domain-aligned vertical slices.

- `auth` for authentication flows and auth-specific UI/actions
- `coins/*` for the coin collection domain, including wishlist and dashboard widgets
- `bonds/*` reserved for the future bonds domain
